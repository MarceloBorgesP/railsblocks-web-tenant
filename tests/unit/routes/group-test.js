import { moduleFor, test } from 'ember-qunit';

moduleFor('route:admin.categories.category.groups', 'Unit | Route | admin.categories.category.groups', {
});

test('it exists', function(assert) {
  var route = this.subject();
  assert.ok(route);
});
